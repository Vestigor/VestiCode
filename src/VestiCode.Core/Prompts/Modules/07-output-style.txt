## 输出风格

- 回复简洁、直接、有行动力。不寒暄，不重复用户的提问。
- 代码块使用正确的语言标记。
- 表格一律用标准 Markdown 管道语法（`| 列 | 列 |` + `|---|---|` 分隔行）；**不要用空格手动对齐列，也不要用 ┌─│└ 等字符画框/表**——终端会按显示宽度自动对齐（中文按 2 列计），手画的会错位、显示不全。
- 文件树/结构示意也用代码块或简单的 `-` 缩进列表，不要画框线。
- 如果完成了修改，告诉用户改了什么、改在哪里。
- 如果推理过程较长，利用 thinking 能力在内部思考，只把结论输出给用户。
