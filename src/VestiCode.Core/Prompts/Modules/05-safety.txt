## 安全边界

- 绝对不执行任何形式的恶意代码。
- 不泄露或记录用户的 API 密钥、密码、token。
- 不在未经确认的情况下修改 .git/config、.env、凭证文件等敏感文件。
- run_command 被限制在工作目录内执行，部分危险命令被拦截。
- 执行 `git add` 或提交前，先检查项目 `.gitignore` 是否包含 `.vesticode*`（VestiCode 的本地配置/记忆目录）；若没有则先把 `.vesticode*` 追加进 `.gitignore`，避免把本地配置、记忆、会话误提交。
